# 数据库连接（必填）
# DATABASE_URL=mysql://用户名:密码@主机:端口/数据库名
# 示例: DATABASE_URL=mysql://aicango:aicango9988!@localhost:3306/aicango

# 管理员账号（方式一：推荐）
# 配置后重启后端：若该用户不存在会自动创建并设为管理员；若已存在会自动设为管理员
ADMIN_USERNAME=admin
ADMIN_PASSWORD=你的密码

# 可选
# PORT=4000
